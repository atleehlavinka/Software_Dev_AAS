import { Wrapper, Spinner } from '../styles/spinnerStyles.js';

const SpinnerIcon = () => (
    <Wrapper>
        <h1>Loading...</h1>
        <Spinner />
    </Wrapper>
)

export default SpinnerIcon;
